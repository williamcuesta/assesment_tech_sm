DROP DATABASE IF EXISTS prueba_sm;
CREATE DATABASE prueba_sm;
USE prueba_sm;

/* first create the db and after create tables */

CREATE TABLE `prueba_sm`.`tabletest` (
    `idejemplo` INT NOT NULL AUTO_INCREMENT,
    `fname_tabletest` varchar(80),
    `lname_tabletest` varchar(80),
    CONSTRAINT pk PRIMARY KEY( `idejemplo`)
);

CREATE TABLE `prueba_sm`.`tabletest2` (
    `idejemplo2` INT NOT NULL AUTO_INCREMENT,
    `fname_tabletest2` varchar(80),
    `lname_tabletest2` varchar(80),
    CONSTRAINT pk_t2 PRIMARY KEY( `idejemplo2`)
);